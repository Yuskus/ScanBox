qin:

dotnet ef migrations add TryToFinish --project ..\DatabaseModel --context ScanBoxDbContext

dotnet ef database update --connection "Host=localhost;Username=postgres;Password=example;Database=ScanBoxDb"
or
dotnet ef database update --connection "Host=localhost;Username=postgres;Password=example;Database=ScanBoxDb" --project ..\DatabaseModel --context ScanBoxDbContext

out:

dotnet ef database update 0 (или имя миграции) (нужен ли коннект?)

dotnet ef migrations remove --project ..\DatabaseModel --context ScanBoxDbContext